Phase 67: Public Benchmark Performance and Claim Promotion
=========================================================

Status
------

[PAUSED_BOUNDARY] The current claim gate has no allowlisted end-to-end benchmark
runner and therefore permits no promotion. All five declarations are paused,
with zero current claims and zero promotable historical evidence. The older
promotion tasks below are retained only as execution history; they do not define
the current gate contract. A future promotion must introduce its producer and
independent verifier together before any README benchmark row can be added.


Goal
----

Turn internal benchmark evidence into honest, reproducible, public-claim-ready
scores. Every claimed benchmark must have a claim report with dataset source,
license, command, commit, model, scorer, and baseline; executionFailures 0; no
same-model judge for public semantic-judge claims; fitted AND generalization
recall reported together for BEAM.


Initial Key Finding (P67-A, 2026-06-24)
---------------------------------------

The claim gate's first verdict over all five benchmarks on 2026-06-24: 0
publicly claimable, each blocked for a precise reason.

- LongMemEval (0.908), ImplicitMemBench (0.711), BEAM (0.695): blocked by a
  SAME-MODEL judge (gpt-5.5 answer + gpt-5.5 judge; .env EVAL and JUDGE are both
  gpt-5.5 @ ai.gurkiai.com) plus unverified provenance/commit.
- MemoryAgentBench (CR 0.959 / AR 0.67): cleanest methodology — deterministic
  upstream match-mode scoring, NO judge, so no same-model bias — but blocked by
  incomplete coverage (TTL 0/30, LRU 1/6) and a missing baseline.
- LoCoMo (0.020): a retrieval boundary, not a performance result.

P67-A INFRA CONSTRAINT: there was no independent judge model available for that
first verdict (only gpt-5.5 was served). So the same-model-judge blocker on
LongMemEval/BEAM could not be cleared by a judge swap at that point. The two
viable JUDGE-FREE public-claim routes were:
  1. MemoryAgentBench — complete TTL/LRU coverage + add a baseline (P67-C); it is
     already deterministic and judge-free.
  2. LongMemEval deterministic subset — report only the judge-free methods
     (abstention/contains/exact/expected_alternative/numeric_count), excluding the
     semantic_judge questions, as a separate honest sub-score (P67-B).
Those were the fastest honest paths; the independent-judge path was infra-blocked
like LoCoMo's neural-embedding path.

Historical verdict after the ImplicitMemBench gpt-5.4 stored-answer rescore
(2026-07-06): all five declarations were then treated as public-claim
candidates. Phase 68 later superseded that boundary: current `v0.6.0` claims
are BEAM, LoCoMo, and MemoryAgentBench; LongMemEval and ImplicitMemBench are
versioned historical evidence under `benchmark-claims/*.json`.
Phase 61 Full-300 rerun evidence is now protected by strict scalar parsing:
`eval:phase-61-full300` rejects duplicate benchmark/output/run/budget selectors,
requires a single path-segment `--run-id`, requires canonical positive integer
budgets, and rejects empty or whitespace-padded ImplicitMemBench root/env
fallbacks before launching live shards. This hardens fresh reruns only; the
public claim boundary is now controlled by the stored-answer gpt-5.4 rescore
declaration and gate.
The 2026-07-06 fixture-root ad hoc rerun
`run-phase61-full300-rerun-20260706-current` covered only 4 cases, not 300, and
is smoke-scale only. The same-day full-root rerun
`run-phase61-full300-rerun-20260706-codex-current` used `/tmp/ImplicitMemBench`
at upstream `927413bf3f5389bb47c94c2a0ba987e435b101b8` (dataset CC BY 4.0,
code MIT), completed all 300 benchmark cases with 0 execution failures, and
measured best
official-comparable GoodMemory full-300 score 212.45/300 = 0.7081666667
(`goodmemory-distilled-feedback+controlled-priming`), raw 179.45/300 =
0.5981666667, distilled blocking 154/200 = 0.77, and baseline 123/300 = 0.41.
This refreshes the canonical source-answer artifact; its 0.7081666667 score is
a same-model diagnostic only, not the public claim.
The postchanges full-root rerun
`run-phase61-full300-rerun-20260706-postchanges-current` completed all 300
cases but had 2 GoodMemory distilled execution failures from
`text_answer_generation timed out after 180000ms`. Its best
official-comparable GoodMemory score was 209.05/300 = 0.6968333333, raw
175.05/300 = 0.5835, distilled blocking 153/200 = 0.765, and baseline
131/300 = 0.4366666667. Treat it as drift evidence only, not as a replacement
for the 0-failure canonical internal rerun and not as public-claim evidence.
The latest-current full-root rerun
`run-phase61-full300-rerun-20260706-latest-current` completed all 300 cases
after the benchmark-hardening work, but had 1 GoodMemory distilled execution
failure. Its best official-comparable GoodMemory score was 211.06/300 =
0.7035333333, raw 179.06/300 = 0.5968666667, distilled blocking 155/200 =
0.775, and baseline 130/300 = 0.4333333333. It improves over the failed
postchanges rerun, but remains drift evidence only because it is still not a
0-failure replacement for the canonical internal rerun.
The after-hardening full-root rerun
`run-phase61-full300-rerun-20260706-after-hardening-current` completed all 300
cases with 0 execution failures while the stored-answer rescore runner was
being hardened, but its best official-comparable GoodMemory score fell to
206.85/300 = 0.6895, raw 174.85/300 = 0.5828333333, distilled blocking
151/200 = 0.755, and baseline 130/300 = 0.4333333333. Treat this as the
freshest clean drift check, not as a better claim artifact; it is 5.60
passed-equivalent points below the canonical `codex-current` internal score and
does not replace the stored-answer comparability artifact.
The stored-answer readiness audit
`implicitmembench-rescore-readiness-20260706-current` proves the canonical
0-failure Full-300 reports are complete enough for no-answer-rerun rescoring:
baseline 300 rows, GoodMemory composite 300 rows, 35 deterministic
`structured_first_action` rows, and 265 judge-required rows. It also records the
current judge env as same-model (`gpt-5.5` answer and judge), so the next
promotion step is independent judging or a broader deterministic scorer, not
answer regeneration.
The cross-version readiness audit
`implicitmembench-rescore-readiness-gpt54-probe-current` records answer
`gpt-5.5`, judge `gpt-5.4`, `sameModelJudge: false`, and
`readyForIndependentJudgeRescore: true` on the same row scope.
The no-answer-rerun command has completed:
`rescore:phase-61-implicitmembench` consumed the canonical overall report,
follows its source baseline/GoodMemory report paths, preserves stored answers,
carries deterministic `structured_first_action` rows unchanged, rejudges the
stored baseline and GoodMemory composite `text_behavior_judge` /
`priming_pair_judge` rows with the configured independent judge, and rebuilds
the Phase 60 Full-300 summary. It rejects same-model answer/judge configuration
before reading source reports, rejects partial or mismatched source scopes before
any judge call, writes `run-identity.json` with source report SHA-256
fingerprints, resumes matching completed judge rows from `progress.jsonl`,
rejects stale, duplicate, or out-of-scope progress rows, and writes
`rescore-summary.json` with explicit unchanged-answer evidence. The completed
`implicitmembench-independent-rescore-gpt54-current` run made 530
judge-required row decisions (265 baseline + 265 GoodMemory composite), had 0
execution failures, and scored GoodMemory 207.35/300 = 0.6911666667 versus
upstream-chat baseline 120/300 = 0.4. This clears the prior same-model blocker,
with required disclosure that gpt-5.4 is cross-version but the same GPT family
as the gpt-5.5 answerer, not a cross-family judge.


Tasks
-----

[DONE] P67-A Public benchmark claim gate (commit 3565877,
  scripts/run-public-benchmark-claim-gate.ts, gate:public-benchmark-claim).
  BenchmarkClaimReport schema + pure rule engine + over-claim detection; committed
  declarations under benchmark-claims/<name>.json; gitignored gate report under
  reports/release/claims/. Unit-tested (7/7). First run: 5 declarations, all
  consistent, 0 over-claiming, 0 publicly claimable.
  CLI guard update (2026-07-04): `gate:public-benchmark-claim` now rejects
  duplicate `--strict` and `--claims-dir` flags before reading claim
  declarations, keeping strict-mode and declaration-source selection
  unambiguous. This does not change the claimable benchmark set.
  Current boundary update (2026-08-13): the strict gate still validates the
  declaration schema and canonical filenames, but diagnostic artifacts cannot
  authorize promotion. Declarations may not supply README presentation
  fragments, current and historical promotion are both unavailable while the
  end-to-end runner allowlist is empty, and both README claim tables must remain
  empty. Internal benchmark reports remain diagnostic inputs only.

[DONE 2026-07-05] P67-A2 Official-protocol rescore CLI guard. The pinned
  package command is `eval:official-rescore`; the underlying comparability
  rescore entrypoint (`scripts/rescore-official-protocols.ts`) now rejects
  duplicate scalar benchmark/source/run/judge selectors before loading
  existing LongMemEval, LoCoMo, or BEAM answer reports for official-protocol
  judging: `--benchmark`, `--reference`, `--report`, `--root`, `--rubrics`,
  `--concurrency`, `--limit`, and `--run-id`. `--run-id` must be a single path
  segment; `--concurrency` and `--limit` must be canonical positive integers.
  It also rejects benchmark-incompatible source selectors before loading reports:
  `--reference` is LongMemEval-only, `--root` is LoCoMo-only, and `--rubrics`
  is BEAM-only.
  Each output run directory now persists `run-identity.json`, rejects malformed
  or unknown-field existing identity files, rejects non-SHA-256 source
  fingerprints, and refuses to reuse an existing `progress.jsonl` cache when
  the benchmark, run id, source input paths, source input SHA-256 fingerprints,
  judge model, slice limit, or stored-answer boundary no longer match.
  Resumable progress rows now reject malformed or unknown-field cached verdicts/scores,
  malformed BEAM rubric keys whose `<questionId>#<itemIndex>` prefix disagrees
  with row `questionId`, duplicate cached question/rubric keys, and cached rows
  outside the selected case/rubric scope instead of silently counting
  non-boolean correctness, invalid BEAM rubric scores, last-write-wins summary
  drift, or stale out-of-scope slice rows, while still tolerating a killed-run
  final partial JSON line. Unit coverage now proves that tolerance for both
  question-level and BEAM rubric progress caches, while still rejecting broken
  JSON lines before the final tail.
  Generated `rescore-summary.json` files also persist `generatedAt`,
  `outputPath`, `sourceInputs`, `sourceInputFingerprints` (bytes + SHA-256),
  `judgeModel`, `limit` (number for a sliced rescore, `null` for full scope),
  `limitUnit` (`cases` for LongMemEval/LoCoMo,
  `rubric-items` for BEAM), `sourceAnswersUnchanged`, source-vs-selected scope
  counts (`sourceCases`/`selectedCases`, or BEAM `sourceQuestions` /
  `selectedQuestions` plus `sourceRubricItems` / `selectedRubricItems`), and an
  benchmark-and-judge-model-aware claim boundary for stored-answer prompt-
  compatible evidence. In particular, LongMemEval scores judged by gpt-5.4 or
  gpt-5.5 are not labeled directly comparable to published official scores.
  Source input paths are also rejected when they resolve inside the
  official-rescore output run directory, so stored answers, benchmark reference
  roots, and BEAM rubrics cannot be mixed with derived `run-identity.json`,
  `progress.jsonl`, or `rescore-summary.json` artifacts. This is input,
  source-family, run-directory, source/output isolation, malformed or
  unknown-field identity, non-SHA-256 source fingerprints, slice-budget,
  progress-cache, and source-lineage hardening for the planned independent judge
  comparability rescore only; it does not regenerate answers, run a judge in
  this guard step, change scores, or change public-claim boundaries.
  Judge gateway environment is now preflighted before creating the output run
  directory or writing `run-identity.json`: `GOODMEMORY_JUDGE_BASE_URL`,
  `GOODMEMORY_JUDGE_API_KEY`, and `GOODMEMORY_JUDGE_MODEL` must be present,
  non-empty, and free of leading/trailing whitespace. This prevents a failed
  env-only dry start from leaving a run identity with an undefined or malformed
  judge model that would later block the real rescore as identity drift.
  Final `rescore-summary.json` writes are now refused if any judge call fails;
  `progress.jsonl` stays resumable under the same run id until the selected
  scope completes without judge failures. This prevents partial official-judge
  batches from being mistaken for complete comparability evidence.
  Follow-up hardening (2026-07-06): the rescore writer now serializes
  question-level progress rows in the same strict `{correct, questionId}` shape
  that the resume parser accepts, and serializes BEAM rubric rows through the
  canonical rubric serializer covered by unit tests. This prevents a new
  LongMemEval/LoCoMo judge batch from writing `category` / raw judge text fields
  that a later strict resume would reject as unknown-field cache drift.
  `rescore-summary.json` is also schema-validated before write: stale summaries
  without stored-answer comparability metadata, source fingerprints,
  selected/source scope counts, complete judged coverage, or in-range score
  fields now fail instead of replacing auditable evidence; summaries whose
  category aggregates do not match the headline counts and scores also fail.
  The validator recomputes
  LongMemEval/LoCoMo category totals, correct counts, per-category accuracy,
  and overall accuracy; for BEAM it recomputes category question totals,
  macro-by-category, and question-weighted micro scores. The existing
  LongMemEval, LoCoMo, and BEAM official-rescore local artifacts were
  refreshed from complete cached progress with `0 to judge`: each now has a
  matching `run-identity.json`, source input SHA-256 fingerprints,
  source-vs-selected scope counts, and an explicit stored-answer comparability
  `claimBoundary` in `rescore-summary.json`, with the same published scores.

[WITHDRAWN 2026-07-31] P67-B LongMemEval replay. The v0.3.5
  `goodmemory-rules-only` replay produced a judge-free deterministic-subset
  result: 0.720 (360/500) versus no-memory 0.068 over the full 500
  questions, `executionFailures: 0`, MIT dataset provenance verified, commit
  `6f9152c`, and same-model semantic-judge rescues excluded from the recorded
  score. It was public at this phase boundary, but a later audit proved that
  `goodmemory-rules-only` consumed answer annotations. It is contaminated
  provenance, not historical evidence.
  CLI guard update (2026-07-04): `eval:phase-62-deterministic-subset` rejects
  duplicate `--report-path`, `--run-id`, `--output-dir`, `--claim-profile`, and
  `--baseline-profile` values before resolving the source report, keeping the
  judge-free helper's source/output/profile selection unambiguous. This does
  not change the versioned 0.720 result.

[DONE 2026-06-25] P67-C MemoryAgentBench task-specific answer harness. The
  deterministic, judge-free task-specific harness completed the four-competency
  run with `executionFailures: 0` over 259 questions. Public claim scope is CR
  0.959 and TTL 0.767 versus no-memory 0.000 baselines; AR 0.890 and LRU 0.518
  are deliberately excluded because no-memory baselines are higher / multiple-
  choice leakage dominates. `benchmark-claims/memoryagentbench.json` now has
  `publicClaimAllowed: true` and passes `gate:public-benchmark-claim`.

[DONE 2026-07-05] P67-D BEAM public-claim re-anchor. The old internal >=0.75
  target was an improvement bar for the internal binary judge before a public
  BEAM reference existed; it is not the public-claim threshold. The public claim standard is now: official protocol + above the public reference + full disclosures.
  `benchmark-claims/beam.json` passes `gate:public-benchmark-claim`
  with official-protocol 0.802 versus the 0.49 public reference, while retaining
  the strict internal binary track (0.7225), cross-version same-family judge
  disclosure, fitted/generalization recall disclosure, no-pack ablation, and the
  instruction_following weak spot. Remaining conflict_update /
  instruction_following / temporal_order answer-gap work stays in the BEAM
  hardening lane and does not block the scoped public claim.

[SUPERSEDED] P67-E LoCoMo neural semantic retrieval probe (P65-R003). Resolved
  NEGATIVE 2026-06-27 (bf7373f: neural == BM25) and superseded by the semantic
  candidate-generation UNION lane (P65-R004/R005, board 70) which broke the
  additive ceiling. See board 70 for the union evidence trail.

[WITHDRAWN 2026-07-31] P67-E2 LongMemEval HYBRID-profile measurement.
  Full 500 rerun of the goodmemory-hybrid profile (provider embedding additive +
  assisted extraction + Postgres; shard-resumable across a machine reboot;
  run-p67e-longmemeval-hybrid-current): judge-free deterministic subset
  0.722 (361/500), executionFailures 0; with-judge diagnostic 0.890.
  The source path was later found to expose gold-bearing raw session IDs, so the
  0.722/0.890 values and comparison with 0.720 are contaminated provenance, not
  evidence. Command retained for historical reconstruction:
  GOODMEMORY_ASSISTED_EXTRACTOR_PROVIDER=openai bun run eval:phase-62-full500
  -- --benchmark-root ~/.goodmemory-longmemeval --profile goodmemory-hybrid
  --shard-concurrency 4 --run-id run-p67e-longmemeval-hybrid-current
  --resume-existing-shards -> eval:phase-62-deterministic-subset --
  --report-path <runDir>/report.json --claim-profile goodmemory-hybrid.

[DONE 2026-07-06] P67-F ImplicitMemBench stored-answer independent-rescore
  runner and artifact. `rescore:phase-61-implicitmembench` is the no-answer-rerun
  path that cleared the remaining same-model judge blocker with the configured
  gpt-5.4 judge. The runner starts from the canonical Phase 61 overall report,
  loads the source baseline and GoodMemory reports, preserves answers, carries
  deterministic `structured_first_action` rows forward, rejudges the stored
  baseline and GoodMemory composite judge-required rows, and writes
  `run-identity.json`, `progress.jsonl`, `baseline-report.json`,
  `goodmemory-report.json`, `overall-summary.json`, and `rescore-summary.json`
  with `sourceAnswersUnchanged: true`. It rejects same-model answer/judge env
  before reading source reports, rejects partial or mismatched source scopes
  before any judge call, and resumes only progress caches whose source report
  SHA-256 fingerprints and judge identity still match. The completed
  `implicitmembench-independent-rescore-gpt54-current` run scored GoodMemory
  207.35/300 = 0.6911666667 versus upstream-chat baseline 120/300 = 0.4, made
  530 judge-required row decisions, and had 0 execution failures. Public use
  must disclose that gpt-5.4 is cross-version but same GPT family as the
  gpt-5.5 answerer, not a cross-family judge.


Acceptance Criteria
-------------------

- All five declarations remain `paused_boundary` with
  `publicClaimAllowed=false`, and each filename matches its canonical benchmark
  slug.
- Current and historical README claim tables contain no benchmark rows.
- Declaration-supplied README presentation is schema-invalid.
- Diagnostic artifacts and stored-answer rescoring cannot open the promotion
  boundary.
- Any future promotion adds one end-to-end producer and an independent verifier
  together, with exact source closure, full coverage, zero execution failures,
  and independent judging where semantic judging is required.
- `gate:public-benchmark-claim --strict` reports five consistent paused
  declarations, zero current claims, zero promotable historical evidence, and
  zero over-claiming.


Source And Evidence Pointers
----------------------------

- Claim gate: scripts/run-public-benchmark-claim-gate.ts (gate:public-benchmark-claim)
- Declarations: benchmark-claims/*.json
- Gate report (gitignored): reports/release/claims/claim-gate-report.json + summary.md
- Release readiness (Phase 66): scripts/run-v0-3-release-readiness.ts
- LoCoMo paused boundary: task-board/70-phase-65-...txt
